# Recipe Repository

frontend package dependencies - frontend/recipe-repository/package.json:
    installation:
        nodejs
        npm
        npx
        bootstrap
        axios
        react-router-dom
    run commands:
        cd frontend
        npm start

